<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>custom</category>
        <client_script><![CDATA[function ($scope, $timeout, spUtil, $location) {
	var c = this;
	c.requestComment = '';
	c.requestItem = '';
	c.needComment = false;
	c.isLoading = true;

	$scope.loadingIndicator = $rootScope.loadingIndicator;
	//Show widget after loaded
	$scope.$on('sp_loading_indicator', function(e, value) {
		if($scope.loadingIndicator && !value){
			c.isLoading = false;
		}
		$scope.loadingIndicator = value;
	});

	//Will be used when change and problem modules are built
	/*spUtil.recordWatch($scope, "change_request", c.data.changeID, function(event) {
		if(event.data.record.state){
			c.server.get({
				action: "stateChangedBackend",
				itemID: event.data.sys_id,
				newState: event.data.record.state,
				requests: c.data.requests
			}).then(function(response) {
				c.data.requests = response.data.requests;
			});
		}
	});

	spUtil.recordWatch($scope, "problem", c.data.problemID, function(event) {
		if(event.data.record.state){
			c.server.get({
				action: "stateChangedBackend",
				itemID: event.data.sys_id,
				newState: event.data.record.state,
				requests: c.data.requests
			}).then(function(response) {
				c.data.requests = response.data.requests;
			});
		}
	});*/

	//Old Record Watch
	spUtil.recordWatch($scope, "sc_req_item", c.data.requestsID, function(event) {
		if(event.data.record.state){
			c.server.get({
				action: "stateChangedBackend",
				itemID: event.data.sys_id,
				newState: event.data.record.state,
				requests: c.data.requests
			}).then(function(response) {
				c.data.requests = response.data.requests;
			});
		}
	});

	spUtil.recordWatch($scope, "incident", c.data.incidentsID, function(event) {
		if(event.data.record.state){
			c.server.get({
				action: "stateChangedBackend",
				itemID: event.data.sys_id,
				newState: event.data.record.state,
				requests: c.data.requests
			}).then(function(response) {
				c.data.requests = response.data.requests;
			});
		}
	});

	$scope.$on('applyFilter', function(event, typeFilter, statusFilter) {
		c.data.test = statusFilter;
		c.isLoading = true;
		c.server.get({
			action: "applyFilter",
			typeFilter: typeFilter,
			statusFilter: statusFilter,
			requests : c.data.requests,
			showFinished : c.data.showFinished,
			user_id : c.data.user_id
		}).then(function(response) {
			c.data.requests = response.data.requests;
			c.data.showFinished = response.data.showFinished;
			$timeout(function() {
				scrollTo("list-columns");
			});
			c.verifyEmptySearch();
		});
	});

	$scope.$on('applyDate', function(event, daysAgo) {
		c.isLoading = true;
		c.server.get({
			action: "applyDate",
			daysAgo: daysAgo,
			requests : c.data.requests
		}).then(function(response) {
			c.data.requests = response.data.requests;
			$timeout(function() {
				scrollTo("list-columns");
			});
			c.verifyEmptySearch();
		});
	});

	$scope.$on('clearAllFilters', function(event) {
		c.isLoading = true;
		c.server.get({
			action: "clearAllFilters",
			requests : c.data.requests
		}).then(function(response) {
			c.data.requests = response.data.requests;
			c.verifyEmptySearch();
		});
	});

	c.verifyEmptySearch = function(){
		c.data.showFilterClean = true;
		for(i in c.data.requests){
			if(c.data.requests[i].show){
				c.data.showFilterClean = false;
			}
		}
	}

	c.itemAction = function(item, action){
		//if(action != "rejectSolution"){
		c.server.get({
			action: 'changeItemState',
			state: action,
			item: item
		}).then(function(response) {
			item = response.data.item;
		});
		/*}
		else{
			$('#requestCommentModal').modal("show");
			c.requestItem = item;
		}*/
	}

	/*c.submitRequestComment = function(){
		if(c.requestComment){
			c.needComment = false;
			c.server.get({
				action : "changeItemState",
				state: "rejectSolution",
				item: c.requestItem
			}).then(function(response) {
				$('#requestCommentModal').modal("hide");
			});
		}
		else{
			c.needComment = true;
		}
	}*/

	c.showDetails = function(item){
		var showDetails = false;
		for(i in c.data.requests){
			if(c.data.requests[i].sys_id == item.sys_id){
				c.data.requests[i].showDetails = !c.data.requests[i].showDetails;
				showDetails = c.data.requests[i].showDetails;
			}
			else{
				c.data.requests[i].showDetails = false;
			}
		}
		if(showDetails){
			c.server.get({
				action: 'getTicketConversationWidget',
				item: item
			}).then(function(response) {
				c.data.ticketConversationWidget = response.data.ticketConversationWidget;
			});
		}

	}

	c.orderBy = function(order, items){
		c.server.get({
			action : "orderBy",
			items : items,
			order : order,
			allColumns : c.data.columns
		}).then(function(response) {
			c.data.columns = response.data.columns;
			c.data.requests = response.data.requests;
		});
	}

	c.statusColor = function(status){
		var statusClass = '';
		if (status == 2){
			statusClass = 'status_flow_empty';
		} else if (status == true) {
			statusClass = 'status_flow_active';
		} else if (!status) {
			statusClass = 'status_flow_completed';
		}
		return statusClass;
	}

	$scope.$on("getSearchedItemsFilter", function (event, data, searchSourceID) {
		c.isLoading = true;
		c.server.get({
			action : "searchResult",
			searchResult : data,
			requests : c.data.requests
		}).then(function(response) {
			c.data.requests = response.data.requests;
			c.verifyEmptySearch();
		});
	});

	$scope.$on("getSearchedItemsFilterClear", function (event) {
		c.isLoading = true;
		c.server.get({
			action: "returnDefaultFilter"
		}).then(function(response) {
			c.data.requests = response.data.requests;
			c.verifyEmptySearch();
		});
	});
}]]></client_script>
        <controller_as>c</controller_as>
        <css>.cart-mobile {
  border: 0;
  box-shadow: none;

  .cart-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 20px;

    a {
      text-align: right;
      display: block !important;
    }
  }

  .cart-item-mobile-info {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    padding: 5px 10px 20px 10px;

    .item-info {
      display: flex;
      flex-direction: column;
    }
    .item-image {
      margin: 0;
    }

    a {
      float: none;
      text-align: center;
    }

    select {
      margin-top: 10px;
      padding: 0px 5px;
      text-align-last: center;
    }
  }  
}

td {
  vertical-align: middle !important;
}

.a-disabled {
  cursor: default;
  color: $btn-link-disabled-color;
}

.media-body {
  .btn-link {
    padding-left: 0;
    padding-right: 0;
    color: $primary;

    &amp;:hover {
      color: darken($primary, 10%);
    }

    &amp;:focus {
      color: darken($primary, 10%);
    }
  }
}

.btn-remove {
  font-size: 18px !important;
}

.action-btn {
  display: block;
  width: 100%;
  margin-top: 5px;

  &amp;.btn-link {
    color: $primary;
  }
}

.list-group {
  max-height: 250px;
  overflow-y: auto;
}

.list-group-item.ng-enter {
  transition: all 1s;
  -webkit-transition: all 1s;
  background-color: #c0dcfa;
}

.list-group-item.ng-enter-active {
  background-color: #fff;
}

.empty-cart {
  font-style: italic;
  text-align: center;
  margin-top: 10px;
  margin-bottom: 7px;
}

.view-carts-link {
  text-align: center;
  display: block;
}

.order-details-container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  margin: 5px 10px 10px 10px;

  .order-details {
    padding: 5px;

    textarea {
      resize: none;
      flex-basis: 100%;
      flex-shrink: 1;
      width: 100%;
      box-sizing: border-box;
    }
  }

  .requested-for-details {
    flex: 1 0 auto;
    max-width: 100%;
  }

  .special-instructions-details {
    textarea {
      height: calc(100% - 25px);
    }
  }
}

.item-image {
  margin-left: auto;
  margin-right: auto;
  margin-top: 10px;
}

.empty-state-content {
  margin-top: 30px;

  &amp;.small-cart {
    margin-top: 0px;
    padding: 10px 20px;
  }

  .fa-shopping-cart {
    color: $gray-light;
    font-size: 80px;
    display: block;
    text-align: center;

    &amp;.small-cart {
      font-size: 40px;
    }
  }

  .h3 {
    text-align: center;
    margin-bottom: 5px;
  }
  .h4 {
    text-align: center;
    margin-bottom: 5px;
  }
  p {
    text-align: center;
    margin-bottom: 20px;
  }

  .btn {
    display: block;
    margin-left: auto;
    margin-right: auto;
    width: 300px;
    max-width: 100%;
    margin-bottom: 35px;
  }
}

.large-checkout {
  display: inline-block;
  float: none !important;
  width: 100%;
  margin-top: 15px;
}

.item-row:hover .btn-clear {
  background-color: #f5f5f5;
}

.sc-btn {
  margin-bottom: 10px;
}
.sc-btn-group {
  clear: right;
}

.sc-remove-btn {
  &amp;:hover {
    background-color: $danger;
    color: #fff;
    border-color: darken($danger, 10%);
  }
}

.btn-group-justified &gt; .btn-group {
  padding: 0 5px;

  .btn {
    border-radius: 4px !important;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
}

.cart-table {
  th {
    position: sticky;
    -webkit-position: sticky;
    top: -1px;
    z-index: 10;
    background-color: $panel-bg;
    box-shadow: 0px 1px 0px $panel-inner-border;
  }
}

@media only screen and (max-width : 992px) {
  .sc-btn, .sc-btn-group {
    width: 100%;
  }
  .item-image {
    max-width: 70px;
  }
}

//All pages
.without_notification {
  padding: 30px 0;
  font-size: 30px;
}

/* Custom Checkout */
.panel {
  padding: 30px 100px;
  box-shadow: 0 0 0 0;
  -webkit-box-shadow: 0 0 0 0;
}

/* Table */
.h3 {
  font-size: 28px;
  color: $dodger-blue;
  margin-top: 20px !important;
  margin-bottom: 30px;
}
.b-b {
  border-bottom: 2px solid $slate-gray;
}
.b-t {
  border: 0;
}
thead {
  color: $slate-gray;
  font-family: globorounded_bk;
  font-size: 13px;
}
.table &gt; thead &gt; tr &gt; th {
  border: 0;
  vertical-align: middle;
}
.table-hover &gt; tbody &gt; tr:hover {
  background-color: white;
}
tbody {
  img {
    width: 50px;
    height: 50px;
  }

  td:nth-child(1) {
    width: 20%;
    padding: 20px;
  }

  td:nth-child(2) {
    width: 20%;
    padding: 20px;
  }

  td:nth-child(3) {
    width: 25%;
  }

  td:nth-child(4) {
    width: 5%;
    padding: 20px;
  }

  td:nth-child(5) {
    width: 30%;
    padding: 20px;

    .request_button {
      margin-right: 50px;
    }
  }

}

/* PAGE 1 - My Requests */
.content_my_requests {
  .buttons {
    position: relative;

    &amp;:hover {
      .tooltip {
        visibility: visible;
        opacity: 1;
      }
    }
    .tooltip_right {
      top: calc(50% - 15px);
      left: 60px;
    }
    .tooltip_bottom {
      bottom: -41px;
      left: -23px;
    }
  }
}
.center-loading {
  position: relative;
  left: 50%;
  .sp-loading-indicator{
    color: $deep-sky-blue !important;
  }
}

.filter-padding{
  padding: 10px 0 !important;
}

.thead_myrequests {
  border-top: 2px solid $slate-gray;
}
.column_product {
  .number_text {
    text-transform: uppercase;
  }
  .item_text {
    font-family: 'globorounded_bd';
    font-size: 18px;
    line-height: 1;
  }
  .more_detail {
    text-decoration: underline;
    font-size: 12px;
  }
}
.column_status {
  .service_title {
    text-transform: uppercase;
    font-family: 'globorounded_bd';
    font-size: 12px;
  }
  .pending {
    color: $coral !important;
  }
  .inProgress {
    color: $yellow-green;
  }
  .resolved {
    color: $deep-sky-blue;
  }
}
.column_opened {
  .item_opened {
    font-size: 12px;
  }
}
.column_sla {
  .sla_description {
    font-size: 12px;
  }
}
.column_buttons {
  display: flex;
  border: 0;

  img {
    width: 50px;
    height: 50px;
    margin-right: 20px !important;
  }
  img:last-child {
    margin-right: 0 !important;
  }
}
.process_flow {
  position: relative;

  .process_flow_title {
    text-transform: uppercase;
    font-family: 'globorounded_bk';
    margin-bottom: 20px;
  }
  .line_flow {
    background-color: $slate-gray;
    width: 2px;
    height: 70%;
    left: 29px;
    z-index: 1;
    position: absolute;
  } 
  .items_flow {
    text-transform: uppercase;
    font-family: 'globorounded_bk';
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    z-index: 2;
    position: inherit;

    .status_flow {
      width: 30px;
      height: 30px;
      font-size: 18px;
      border-radius: 15px;
      display: flex;
      justify-content: center;
      align-items: center;
      margin-right: 15px;
    }
    .status_flow_completed {
      color: white;
      background-color: $slate-gray;
    }
    .status_flow_active {
      color: white;
      background-color: $deep-sky-blue;
    }
    .status_flow_empty {
      color: $slate-gray;
      background-color: white;
      border: 2px solid $slate-gray;
    }
    .status_active {
      color: $deep-sky-blue;
    }
  }
}

/* PAGE 2 - Notifications */
.content_notification {
  .header_message {
    padding: 20px 0;
    font-family: 'globorounded_bd';
    font-size: 18px;
    line-height: 1;
    border-bottom: 2px solid $deep-sky-blue;
  }
  .widget_notifications {
    overflow: auto;

    .border-notification {
      padding: 20px 0;
      border-bottom: 2px solid $slate-gray;
    }
  }

  .close {
    font-size: 36px;
    font-family: 'globorounded_lt';
    margin-right: 20px;
    margin-top: 15px;
  }
  .modal-body {
    padding: 60px 100px;

    .modal-title {
      color: $deep-sky-blue;
      font-size: 30px;
    }
    .modal-description {
      font-size: 24px;
      font-family: 'globorounded_bd';
    }
  }
  .form-control {
    width: 329px !important;
  }
}

/* PAGE 3 - APPROVALS */
.approval_content {
  .approval_column_title{
    h4 {
      margin: 4px 0 0 0 !important;
    }
    .approval_number{
      font-size: 16px;
    }
    .approval_title{
      font-family: $text-bold;
    }
    .approval_more-details{
      font-size: 12px;
      text-decoration: underline;
      color: $slate-gray;
      cursor: pointer;
    }
  }
  .approval_column_requester{
    padding-left: 10px !important;
    .info-size{
      height: 20px;
      width: 20px;
      margin-left: 10px;
      margin-top: -5px;
      cursor: pointer;
    }
  }
  .approval_column_opened{
    padding-left: 20px !important;

  }
  .approval_column_forecast{
    padding-left: 12px !important;

  }
  .approvals-buttons{
    .submit-button{
      margin-left: 20px;
    }
    .cancel-button{
      margin-left: 15%;;
    }
  }
  .modal-body{
    overflow: hidden !important;
    border: 20px solid white;
    padding-left: 50px !important;
    .wrapper-xl {
      padding: 50px 0 0 50px !important;
      margin-right: -20px;
    }
    button{
      text-align: right;
      font-size: 42px;
    }
    .justify-text{
      color: $deep-sky-blue;
      font-size: 36px;
      padding-bottom: 15px;
      margin-bottom: 5px;
    }
    .no-text-answer{
      font-size: 16px;
      color: lightcoral;
      padding: 2px;
    }
    .form-group{
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      textarea{
        margin-top: 5px;
        margin-right: 10% !important;
        resize: none;
        overflow: hidden;
        height: 140px;
        width: 90%;
        overflow-wrap: break-word;
        border: solid 3px $deep-sky-blue;
      }
    }
    .resize-img{
      height: 70px;
      margin-top: 25px;
      margin-left: -110%;
    }
  }
}

/* PAGE 4 */
.assets_content {
  .column_name {
    .asset_name {
      font-size: 18px;
      font-family: 'globorounded_bd';
    }
  }
  .service_title {
    font-size: 12px;
    color: $slate-gray;
  }
}

/* PAGE 5 */
.page_description {
  border-bottom: 2px solid $slate-gray;
  padding: 20px;
  font-family: 'globorounded_bd';
  font-size: 16px;
  line-height: 1;
}
.column_category {
  text-align: center;

  h4 {
    text-transform: uppercase;
    font-size: 14px;
    font-family: globorounded_bk;
    color: $dodger-blue;
    width: 130px;
    margin: 0 auto;
    margin-top: 5px;
  }
}
.column_service {
  color: $dodger-blue;

  .service_title {
    font-size: 12px;
    text-transform: uppercase;
  }
  .service_description {
    font-size: 14px;
    font-weight: bold;
  }
}
.column_description {
  .description {
    font-size: 12px;
  }
}
.column_sla {
  .sla_text {
    font-size: 12px;
  }
}


.footer_checkout {

  .continue_shopping {
    color: $slate-gray;
    font-size: 10px;
    font-weight: bold;
    text-decoration: underline;
    text-transform: uppercase;
    padding: 20px 0 !important;
  }

  .submit_button {
    .submit_button_content {
      display: flex;
      align-items: center;

      .submit_img {
        width: 50px;
        height: 50px;
        margin-right: 5px;
      }
      .submit_title {
        font-size: 20px;
        font-weight: bold;
        line-height: 1;
        color: $dodger-blue;
        width: 90px;
      }
    }
  }
}

.empty_content {
  background-color: white;
  padding: 30px 100px;
  padding-bottom: 330px;
  font-size: 30px;

  .empty_title {
    color: $dodger-blue;
    margin-bottom: 30px;
  }
}

.column_content {
  img {
    height: 10px;
    position: relative;
    top: -2px;
    right: -2px;
  }
  .invert-arrow{
    transform: rotateX(180deg);
  }
}

.content_conversation {
  width: 100%;
}</css>
        <data_table>sp_instance</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list/>
        <has_preview>false</has_preview>
        <id>gservice_my_dashboard_requests</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {
  
}]]></link>
        <name>GService My Dashboard Requests</name>
        <option_schema>[{"hint":"Selects the columns to show on the table","name":"table","section":"Presentation","label":"Table","type":"string"},{"name":"table_template","section":"Behavior","label":"Table Template","type":"string"}]</option_schema>
        <public>false</public>
        <roles/>
        <script><![CDATA[(function() {
	data.taskQuery = "state=-5^u_pending_reason=1^request_item.opened_by=" + gs.getUserID();
	data.incidentQuery = "state=3^hold_reason=1^opened_by=" + gs.getUserID();
	var localInput = input; //to safeguard pullution of "input" via BR or other scripts
	var portalUtils = new global.OCLPortalUtils();
	data.requestsID = '';
	data.showEmptyMessage = false;

	data.statusFlow = [];
	var statusFlow = gs.getProperty("gservice_list_filters_status").split("|");
	for(i in statusFlow){
		statusFlow[i].number = i + 1;
		statusFlow[i].selected = false;
		data.statusFlow.push(JSON.parse(statusFlow[i]));
	}

	//Used to get the current user sys_id
	var userID = gs.getUser().getID();
	data.user_id = userID;

	data.listFilter_widget = $sp.getWidget('gservice_list_filter');

	data.filter = false;
	//Get the columns (from property) to show upside the requests
	data.columns = [];

	if(!localInput){
		//You need to select the columns name on the property
		//Using checkout as a test, but it`ll change to a dinamic way when you call the widget you send the option
		var columns_property = gs.getProperty("gservice_dashboard_columns");

		var parse = JSON.parse(columns_property);
		var columns = parse["requestsTab"].columns;

		data.columns = columns;
		for(i in columns){
			data.columns[i].name = gs.getMessage(columns[i].name);
		}
		var requests = portalUtils.getRequestsv2(data.user_id, '', false);
		data.requests = requests.items;
		data.showFinished = requests.showFinished;
		if(data.requests.length == 0){
			data.showEmptyMessage = true;
		}
		prepareRecordWatch();

	}

	else{
		switch(localInput.action){
			case "applyFilter":
				data.requests = localInput.requests;
				var statusFilter = localInput.statusFilter;
				data.showFinished = localInput.showFinished;
				if(!localInput.showFinished){
					
						if(statusFilter['Cancelado'] || statusFilter['Finalizado Completo']){
							var newRequests = portalUtils.getRequestsv2(localInput.user_id, data.requests, true);
							data.requests = newRequests.items;
							data.showFinished = newRequests.showFinished;
						}
				}
				data.requests = portalUtils.filterRequests(data.requests, localInput.typeFilter, localInput.statusFilter);
				for(i in data.requests){
					data.requests[i].showDetails = false; 
				}
				break;

			case "changeItemState":
				data.item = portalUtils.changeItemState(localInput.item, localInput.state);
				break;
			case "stateChangedBackend":
				var state = localInput.newState;
				var itemID = localInput.itemID;
				data.requests = localInput.requests;
				for(i in data.requests){
					if(data.requests[i].sys_id == itemID){
						data.requests[i].statusLabel = state.display_value;
						data.requests[i].statusValue = state.value;
						data.requests[i] = portalUtils.verifyButtons(data.requests[i]);
						data.requests[i].colorClass = portalUtils.getColorClass(data.requests[i].statusValue,data.requests[i].table);
					}
				}
				break;

			case "applyDate":
				data.requests = portalUtils.applyDate(localInput.requests, localInput.daysAgo);
				break;

			case "clearAllFilters":
				data.requests = localInput.requests;
				for (i in data.requests){
					data.requests[i].show = false;
				}
				break;

			case "returnDefaultFilter":
				data.requests = portalUtils.getRequests(data.user_id, data.filter);
				prepareRecordWatch();
				break;

			case "getTicketConversationWidget":
				var gservice_ticket_conversation_options = {table : localInput.item.table,
																										sys_id : localInput.item.sys_id,
																										item_status : localInput.item.colorClass};
				data.ticketConversationWidget = $sp.getWidget('gservice_custom_conversation', gservice_ticket_conversation_options);
				break;

			case "orderBy":
				var order = localInput.order;
				data.columns = localInput.allColumns;
				var direction;
				for(i in data.columns){
					if(order == data.columns[i].order){
						data.columns[i].direction = !data.columns[i].direction ? -1 : -data.columns[i].direction;
						direction = data.columns[i].direction;
					}
					else{
						data.columns[i].direction = false;
					}
				}
				var items = localInput.items;
				var itemsSort = portalUtils.sortList(items, localInput.order, direction);
				data.requests = itemsSort;
				break;
			case "searchResult":
				data.requests = localInput.requests;
				prepareRecordWatch();
				data.requests = portalUtils.searchMade(data.requests, localInput.searchResult);
				break;
		}
	}

	function prepareRecordWatch(){
		data.requestsID = false;
		data.incidentsID = false;
		for(i in data.requests){
			if(data.requests[i].show){
				if(data.requests[i].table == 'sc_req_item'){
					if(!data.requestsID){
						data.requestsID = "sys_id=" + data.requests[i].sys_id;
					}
					else{
						data.requestsID += "^ORsys_id=" + data.requests[i].sys_id;
					}
				}
				else if(data.requests[i].table == 'incident'){
					if(!data.incidentsID){
						data.incidentsID = "sys_id=" + data.requests[i].sys_id;
					}
					else{
						data.incidentsID += "^ORsys_id=" + data.requests[i].sys_id;
					}
				}
			}
		}
	}
})();]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>organize.fernando</sys_created_by>
        <sys_created_on>2020-06-03 18:48:13</sys_created_on>
        <sys_id>88e8a47fdb099450fa11cf2414961937</sys_id>
        <sys_mod_count>44</sys_mod_count>
        <sys_name>GService My Dashboard Requests</sys_name>
        <sys_package display_value="ITSM Service Portal" source="x_gmeps_itsm_servi">334ae627dbc85c105c1163901496192f</sys_package>
        <sys_policy/>
        <sys_scope display_value="ITSM Service Portal">334ae627dbc85c105c1163901496192f</sys_scope>
        <sys_update_name>sp_widget_88e8a47fdb099450fa11cf2414961937</sys_update_name>
        <sys_updated_by>organize.fernando</sys_updated_by>
        <sys_updated_on>2020-06-04 14:22:57</sys_updated_on>
        <template><![CDATA[<div ng-show="data.showEmptyMessage" class="without_notification">
  Você ainda não tem solicitações.
</div>
<div ng-show="!data.showEmptyMessage">
  <div class="col-md-12 filter-padding">
    <sp-widget widget="data.listFilter_widget"></sp-widget>
  </div>

  <div ng-if="data.showFilterClean" class="without_notification">
    ${Ops! Não encontramos resultados para essa busca.}
  </div>

  <div ng-if="!data.showFilterClean" class="content_my_requests">
    <div>
      <table class="table table-hover b-t table-condensed cart-table">

        <div class="center-loading">
          <div class="col-md-12" ng-show="c.isLoading">
            <div class="hidden-xs sp-loading-indicator" ng-if="loadingIndicator">
              <div></div>
              <div></div>
              <div></div>
            </div>
          </div>
        </div>

        <thead ng-show="!c.isLoading" class="thead_myrequests" id="list-columns">
          <tr>
            <th class="col-lg-2 wrapper column_content" ng-repeat="column in data.columns">
              <div ng-click="c.orderBy(column.order, data.requests)">
                {{column.name}}
                <span ng-if="column.order">
                  <span ng-if="!column.direction"></span>
                  <span ng-if="column.direction"><img src="gservice_column_dashboard_arrow.png" ng-class="column.direction == -1 ? 'invert-arrow' : ''"}/></span>
                </span>
              </div>
            </th>
          </tr>
        </thead>

        <tbody ng-show="!c.isLoading" ng-repeat="item in c.data.requests">
          <!-- Requests Init -->
          <tr class="item-row b-b" ng-show="item.show">
            <td data-th="Product" class="column_product">
              <div>
                <div>
                  <!-- Necessário realizar query para buscar a imagem da categoria e categoria do parent-->
                  <div class="number_text">#{{item.number}}</div>
                  <div class="item_text">{{item.title}}</div>
                  <div ng-if="item.watch">${GService FollowUp} {{item.watch}}</div>
                  <div class="more_detail" ng-click="c.showDetails(item)">${GService Details}</div>
                </div>
              </div>
            </td>
            <td class="column_status">
              <div class="row">
                <div>
                  <div class="service_title {{item.colorClass}}">{{item.statusLabel}}</div>
                </div>
              </div>
            </td>
            <td class="column_opened">
              <div class="item_opened">{{item.opened}}</div>
            </td>
            <td class="column_sla">
              <p class="sla_description">SLA: {{item.delivery_time}}</p>
            </td>
            <td class="column_buttons">
              <div ng-if="item.eraseButton" class="buttons" ng-click="c.itemAction(item, 'cancelItem')"><img src="trash.png"/>
                <span class="tooltip tooltip_right">
                  <div class="tooltip_arrow arrow_left"></div>
                  <div class="tooltip_text">${Cancel Request}</div>
                </span>
              </div>
              <div ng-if="item.acceptButton" class="buttons" ng-click="c.itemAction(item, 'acceptSolution')"><img src="gservice_dashboard_confirm.png"/>
                <span class="tooltip tooltip_bottom">
                  <div class="tooltip_arrow arrow_top"></div>
                  <div class="tooltip_text">${Accept Solution}</div>
                </span>
              </div>
              <div ng-if="item.reopenButton" class="buttons" ng-click="c.itemAction(item, 'rejectSolution')"><img src="gservice_reopen.png"/>
                <span class="tooltip tooltip_right">
                  <div class="tooltip_arrow arrow_left"></div>
                  <div class="tooltip_text">${Reopen Request}</div>
                </span>
              </div>
            </td>
          </tr>

          <tr>
            <td ng-if="item.showDetails" class="content_conversation" colspan='5'>
              <div class="col-md-5 process_flow">
                <div class="process_flow_title">Etapas do Atendimento</div>
                <div class="line_flow"></div>
                <div ng-repeat="status in item.processFlow" class="items_flow">
                  <div class="status_flow" ng-class="c.statusColor(status.selected)">{{$index+1}}</div>
                  <div ng-class="{'status_active' : status.selected == true}">{{status.name}}</div>
                </div>
              </div>
              <div class="col-md-7">
                <sp-widget widget="c.data.ticketConversationWidget"></sp-widget>
              </div>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>

<!-- Modal para widget de abertura de formulário, chamado pela função nextModal -->
<!--<div class="modal fade" style="z-index: 1041" id="requestCommentModal" tabindex="-1" role="dialog" aria-labelledby="articleModalLabel" aria-hidden="true">
<div class="modal-dialog modal-lg" role="document">
<div class="modal-content">
<div class="modal-body">
<button type="button" class="close" data-dismiss="modal" aria-label="Close">
<span aria-hidden="true">&times;</span>
</button>
<div class="panel-body wrapper-xl wrapper-sc-cat-item">
<div class="justify-text">${Justifique sua resposta negativa:}</div>
<span class="notification-align no-text-answer" ng-if="c.needComment"> ${Favor preencher o comentário antes de submeter} </span>
<div class="form-group">
<textarea class="form-control" ng-model="c.requestComment" maxlength="255"></textarea>
<a class="" style="cursor: pointer;" aria-label="" ng-click="c.submitRequestComment()" title="">
<img class="resize-img" src="gservice_dashboard_submit.png"/>
</a>
</div>
</div>
</div>
</div>
</div>
</div>-->


  <now-message key="Done" value="${Done}"/>
</div>]]></template>
    </sp_widget>
</record_update>
